# TODO - Login Credentials, Inventory Search & Sort

## Steps
- [x] Add `signUp()` method to `AuthService` for creating login credentials
- [x] Create `supabase_profiles_schema.sql` for the `profiles` table
- [x] Add Sign-Up flow to `login_screen.dart`
- [x] Add design-wise sort options to `inventory_screen.dart`
- [x] Add search-by-design-name field to `inventory_screen.dart`
- [x] Run `flutter analyze` and confirm no errors
- [x] Fix pre-existing `const TextCellValue` build errors in `bulk_inventory_service.dart`
